name: currency_converter
description: A Flutter project for real-time currency conversion.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6  
  google_mlkit_text_recognition: ^0.13.0
  camera: ^0.11.0+1
  path_provider: ^2.1.3
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/img6.jpg
    - assets/img5.jpg
    - assets/img8.jpeg
    
